{
	"name": "gutenberg-ui-playground",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"build": "wp-scripts build",
		"start": "wp-scripts start",
		"build:zip": "npm run build && mkdir -p dist && rm -f dist/gutenberg-ui-playground.zip && zip -r dist/gutenberg-ui-playground.zip gutenberg-ui-playground.php build/ -x '*.map'"
	},
	"devDependencies": {
		"@wordpress/scripts": "^31.2.0"
	},
	"dependencies": {
		"@babel/standalone": "^7.28.5",
		"@base-ui/react": "^1.0.0",
		"@wordpress/element": "*",
		"@wordpress/theme": "*",
		"@wordpress/ui": "*",
		"clsx": "^2.1.1"
	}
}
